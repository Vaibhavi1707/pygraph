def depth_first_search(self, src = None):
    """
    Runs DFS
    Args:
        src (int, optional): Source vertex from where DFS is to be performed. 
                            Defaults to None.
    
    Returns: discovery_times, finish_times, parents 
        tuple(List(int), List(int), List(int)): The discovery and finish times and 
            parents for all vertices of the graph when DFS is run.
    """
    pass

def dfs_forest(self, src = None):
    """
    Returns the list of DFS trees formed as a result of running DFS on a graph.

    Args:
        src ([int, optional): Source from where DFS is to be performed. 
                            Defaults to None.
    
    Returns:
        List(Graph): List of trees obtained in DFS.
    """
    pass

def topological_sort(self):
    """
    Returns:
        List(int): Topological sort of vertices of a graph.
    """
    pass

def is_cyclic(self):
    """
    Returns:
        bool: True is graph is cyclic else False.
    """
    pass
    